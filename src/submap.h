#ifndef __SUBMAP_H__  // prevents the includes from being called twice
#define __SUBMAP_H__

#include <gb/gb.h>

#include "../res/tiles/brick_wide_map.h"
#include "../res/tiles/brick_wide_tiles.h"

void init_submap();

#endif